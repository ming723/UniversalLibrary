<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.abner.ming.abnerlibrary">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- 网络状态权限 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application android:allowBackup="true" android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".net.NetTestActivity"/>
        <activity android:name=".gesturelock.GestureEditActivity"/>
        <activity android:name=".gesturelock.GestureMainActivity"/>
        <activity android:name=".gesturelock.GestureVerifyActivity"/>
    </application>

</manifest>
